{
  "name": "rabbit-skills-website",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate": "node scripts/generate-content.mjs",
    "dev": "npm run generate && vite",
    "build": "npm run generate && tsc --noEmit && vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@fontsource/jetbrains-mono": "^5.1.0",
    "@fontsource/space-grotesk": "^5.1.0",
    "gsap": "^3.13.0",
    "marked": "^15.0.0"
  }
}
