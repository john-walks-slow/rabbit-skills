---
name: workflow-manage-tasks
description: 任务分派工作流。当用户一次提出多条任务、需要协调多个子代理时使用。
user-invocable: true
---

# workflow-manage-tasks

作为项目经理接受用户输入的任务。梳理任务并安排对应子代理实施。

遵守以下工作流程：

## 1. 理解与梳理

逐条理解用户意图，不要遗漏任何细节。熟悉任务涉及的概念和功能，理解任务范围，形成对工作量的评估。如果有直接相关，适合在同一个会话中完成的任务，合并为单一项。

以精准、清晰的语言重新描述任务，并按以下类型归类：

| 类型       | 特征                               | 分派     |
| ---------- | ---------------------------------- | -------- |
| 简单问题类   | 问题原因较明确，容易单点修复 | iterator |
| 优化点类   | 解决方案较明确，容易单点优化 | iterator |
| 疑难问题类 | 原因不明或逻辑复杂，需深入分析排查 | bugfixer |
| 新需求类   | 中等以上规模的新功能               | 默认仅整理不分派（用户自行推动）  |
| 架构类     | 架构重构                           | 默认仅整理不分派  |
| 调研类     | 预研和信息搜集                     | 默认仅整理不分派  |
| 暂缓       | 有外部阻塞或用户意图不清晰         | N/A      |

输出到 `docs/tasks/yymmdd-{summary}.tasks.md`，表格包含以下列，方便用户跟踪：

| 任务 | 类型 | 状态 | 分派至 | 产出文档 |
| ---- | ---- | ---- | ------ | -------- |

状态列使用：`待开始` / `进行中` / `完成` / `待评估` / `待验证` / `阻塞` / `user-owned`。
产出文档列链接子代理交付的 plan / summary / research / validation 等文档。
表格按类型排序（低风险类 → 疑难问题类 → 新需求类 → 架构/调研类 → 暂缓）。

任务间若有依赖项则按照依赖关系设计分发顺序。

**请用户审阅任务理解、归类、分发策略是否正确后才进入下一步。**

## 2. 分派子代理

使用 background 模式并行分派子代理，每一任务使用独立子代理，除 hard dependency 外尽量并行（对并行安全性默认保持乐观）。

**所有具体任务全部分配给子代理执行**，包括但不限于：代码编写、测试、提交。你自身不执行任何具体任务。

**不要擅自为子代理设定 verbose 的提示词和工作方向**——子代理本身已有专业的工作方式，过度限定反而限制其发挥。将经过梳理的简短、清晰、完整的意图传递给子代理即可。

分派时必须向子代理备注：

“遇到需要主代理评估、待用户确认的步骤，或其他阻塞，须暂停工作向上报告。”

## 3. 跟踪与推进

在 `docs/tasks/yymmdd-{summary}.tasks.md` 中持续跟踪所有任务进展，不要遗漏。

根据子代理输出的计划或结果判断其工作是否达标。阶段性结果达标且推进风险较低时，可予以下一步指令推进其继续工作。在验证和检视都通过后，可准许子代理提交代码。

对于具有一定风险需要用户评估的内容（包括：确认问题定位是否正确、对齐计划、验证结果、协助定位等），立刻询问用户，并在询问中附上子代理产出的相关文档链接（如 plan / summary / research / validation 等），方便用户审阅决策。**

**备注:**

当用户书面要求时，你可以调整工作流。例如：跳过用户审阅步骤等。
